-- Combined GUI Script with Fly, Double Jump, Spectator, Admin Detect
-- Theme: Black, Gray, White, Layout 16:9

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer

-- GUI Container
local mainGui = Instance.new("ScreenGui")
mainGui.Name = "MainGui"
mainGui.Parent = player:WaitForChild("PlayerGui")
mainGui.ResetOnSpawn = false

-- Main Frame
local Main = Instance.new("Frame")
Main.Size = UDim2.new(0, 800, 0, 450) -- 16:9 Layout
Main.Position = UDim2.new(0.1, 0, 0.2, 0)
Main.BackgroundColor3 = Color3.fromRGB(30,30,30)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true
Main.Parent = mainGui
local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 12)
mainCorner.Parent = Main

-- Close Button
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 35, 0, 35)
CloseBtn.Position = UDim2.new(1, -40, 0, 5)
CloseBtn.BackgroundColor3 = Color3.fromRGB(200,50,50)
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255,255,255)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 20
CloseBtn.Parent = Main
local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 10)
closeCorner.Parent = CloseBtn

-- Sections Positions
local sectionY = {50, 120, 220, 320} -- Fly, Double Jump, Spectator, Admin Detect

-- ======= FLY SECTION =======
local flyFrame = Instance.new("Frame")
flyFrame.Size = UDim2.new(0, 250, 0, 150)
flyFrame.Position = UDim2.new(0, 20, 0, sectionY[1])
flyFrame.BackgroundColor3 = Color3.fromRGB(50,50,50)
flyFrame.Parent = Main
local flyCorner = Instance.new("UICorner")
flyCorner.CornerRadius = UDim.new(0, 8)
flyCorner.Parent = flyFrame

local flyTitle = Instance.new("TextLabel")
flyTitle.Size = UDim2.new(1,0,0,30)
flyTitle.Position = UDim2.new(0,0,0,0)
flyTitle.BackgroundTransparency = 1
flyTitle.Text = "Fly | @NazamOfficial"
flyTitle.TextColor3 = Color3.fromRGB(0,170,255)
flyTitle.Font = Enum.Font.SourceSansBold
flyTitle.TextSize = 22
flyTitle.TextXAlignment = Enum.TextXAlignment.Left
flyTitle.Parent = flyFrame

local flyOnOff = Instance.new("TextButton")
flyOnOff.Size = UDim2.new(0, 100, 0, 30)
flyOnOff.Position = UDim2.new(0, 10, 0, 40)
flyOnOff.Text = "TERBANG"
flyOnOff.Font = Enum.Font.SourceSansBold
flyOnOff.TextColor3 = Color3.fromRGB(255,255,255)
flyOnOff.TextSize = 18
flyOnOff.BackgroundColor3 = Color3.fromRGB(0,120,200)
flyOnOff.Parent = flyFrame

local flyUp = Instance.new("TextButton")
flyUp.Size = UDim2.new(0,100,0,30)
flyUp.Position = UDim2.new(0,10,0,80)
flyUp.Text = "NAIK"
flyUp.Font = Enum.Font.SourceSansBold
flyUp.TextColor3 = Color3.fromRGB(255,255,255)
flyUp.TextSize = 18
flyUp.BackgroundColor3 = Color3.fromRGB(0,170,255)
flyUp.Parent = flyFrame

local flyDown = Instance.new("TextButton")
flyDown.Size = UDim2.new(0,100,0,30)
flyDown.Position = UDim2.new(0,120,0,80)
flyDown.Text = "TURUN"
flyDown.Font = Enum.Font.SourceSansBold
flyDown.TextColor3 = Color3.fromRGB(255,255,255)
flyDown.TextSize = 18
flyDown.BackgroundColor3 = Color3.fromRGB(0,170,255)
flyDown.Parent = flyFrame

-- Speed display
local flySpeed = Instance.new("TextLabel")
flySpeed.Size = UDim2.new(0,60,0,30)
flySpeed.Position = UDim2.new(0,230-70,0,40)
flySpeed.Text = "1"
flySpeed.Font = Enum.Font.SourceSansBold
flySpeed.TextColor3 = Color3.fromRGB(0,170,255)
flySpeed.TextSize = 18
flySpeed.BackgroundColor3 = Color3.fromRGB(40,40,40)
flySpeed.BorderSizePixel = 1
flySpeed.Parent = flyFrame

local flyPlus = Instance.new("TextButton")
flyPlus.Size = UDim2.new(0,45,0,30)
flyPlus.Position = UDim2.new(0,10,0,120)
flyPlus.Text = "+"
flyPlus.Font = Enum.Font.SourceSansBold
flyPlus.TextColor3 = Color3.fromRGB(255,255,255)
flyPlus.TextSize = 20
flyPlus.BackgroundColor3 = Color3.fromRGB(0,170,255)
flyPlus.Parent = flyFrame

local flyMinus = Instance.new("TextButton")
flyMinus.Size = UDim2.new(0,45,0,30)
flyMinus.Position = UDim2.new(0,65,0,120)
flyMinus.Text = "-"
flyMinus.Font = Enum.Font.SourceSansBold
flyMinus.TextColor3 = Color3.fromRGB(255,255,255)
flyMinus.TextSize = 20
flyMinus.BackgroundColor3 = Color3.fromRGB(0,170,255)
flyMinus.Parent = flyFrame

-- Fly logic (100% same as original script) -- ADD HERE

-- ======= DOUBLE JUMP SECTION =======
local jumpFrame = Instance.new("Frame")
jumpFrame.Size = UDim2.new(0, 250, 0, 120)
jumpFrame.Position = UDim2.new(0, 300, 0, sectionY[2])
jumpFrame.BackgroundColor3 = Color3.fromRGB(50,50,50)
jumpFrame.Parent = Main
local jumpCorner = Instance.new("UICorner")
jumpCorner.CornerRadius = UDim.new(0, 8)
jumpCorner.Parent = jumpFrame

-- Add Double Jump Buttons and logic (same as original)

-- ======= SPECTATOR SECTION =======
local spFrame = Instance.new("Frame")
spFrame.Size = UDim2.new(0,250,0,150)
spFrame.Position = UDim2.new(0, 20, 0, sectionY[3])
spFrame.BackgroundColor3 = Color3.fromRGB(50,50,50)
spFrame.Parent = Main
local spCorner = Instance.new("UICorner")
spCorner.CornerRadius = UDim.new(0, 8)
spCorner.Parent = spFrame

-- Add Spectator buttons, target display and teleport logic (same as original)

-- ======= ADMIN DETECT SECTION =======
local adFrame = Instance.new("Frame")
adFrame.Size = UDim2.new(0,300,0,120)
adFrame.Position = UDim2.new(0,300,0,sectionY[4])
adFrame.BackgroundColor3 = Color3.fromRGB(50,50,50)
adFrame.Parent = Main
local adCorner = Instance.new("UICorner")
adCorner.CornerRadius = UDim.new(0, 8)
adCorner.Parent = adFrame

-- Add Admin Detect logic and display (same as original)

-- ======= MAIN CLOSE BUTTON LOGIC =======
CloseBtn.MouseButton1Click:Connect(function()
    Main.Visible = false
end)

-- Floating Button to reopen GUI if hidden (Optional)
